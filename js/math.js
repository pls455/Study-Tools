
document.addEventListener("DOMContentLoaded",()=>{$("#percent")?.addEventListener("click",()=>{let p=+$("#p").value,n=+$("#n").value;$("#pr").textContent=isFinite(p*n)?`${p}% من ${n} = ${p*n/100}`:"تحقق من القيم"});$("#quad")?.addEventListener("click",()=>{let a=+$("#qa").value,b=+$("#qb").value,c=+$("#qc").value,d=b*b-4*a*c;$("#qr").textContent=!a||d<0?"لا توجد جذور حقيقية":`x₁=${(-b+Math.sqrt(d))/(2*a)} ، x₂=${(-b-Math.sqrt(d))/(2*a)}`});$("#trig")?.addEventListener("click",()=>{let x=+$("#tx").value,f=$("#tf").value,r=$("#tr").value==="deg"?x*Math.PI/180:x;$("#trr").textContent=Math[f](r)});$("#log")?.addEventListener("click",()=>{let x=+$("#lx").value,b=+$("#lb").value;$("#lr").textContent=Math.log(x)/Math.log(b)})})
